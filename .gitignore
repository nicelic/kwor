.DS_Store
dist/
release/
releases/
tmp/
.sync*
*.tar.gz
*.zip

# Old frontend submodule placeholder (project uses temp_frontend/)
frontend/

# Build output embedded into the Go binary (regenerated by build)
web/html/

# Compiled binaries / build artifacts
*.exe
kwor
sui
main
.build-marker
.vscode_build_test.exe

# Runtime data (created next to the executable at runtime)
Promanager_data/
db/
bin/
backup/
_tmp_mihomo_src/
_tmp_mieru_official/

# Backup / temporary source files
*.pre_fix_backup

# Legacy s-ui distribution files (kept locally, not published)
s-ui.service
s-ui.sh
runSUI.sh
Dockerfile.frontend-artifact

# Node modules
node_modules/

# local env files
.env.local
.env.*.local

# Log files
*.log*
.cache

# Editor directories and files
.idea
.vscode
